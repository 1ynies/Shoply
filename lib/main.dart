import 'package:flutter/material.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:shoplyapp/features/AUTH/presentation/bloc/auth_bloc.dart';
// import 'package:hive_flutter/hive_flutter.dart'; // Uncomment when ready

// --- Core Imports ---
import 'firebase_options.dart'; // Generated by FlutterFire
import 'core/di/injection_container.dart' as di; // Dependency Injection
import 'core/router/app_router.dart'; // GoRouter Config
import 'core/theme/app_theme.dart'; // App Theme

// --- Feature Imports ---


void main() async {
  // 1. Ensure native bindings are initialized
  WidgetsFlutterBinding.ensureInitialized();

  // 2. Initialize Firebase
  await Firebase.initializeApp(
    options: DefaultFirebaseOptions.currentPlatform,
  );

  // 3. Initialize Dependency Injection (GetIt)
  await di.initDependencies();

  // --- HIVE INITIALIZATION (Commented out as per your code) ---
  // await Hive.initFlutter();
  // Hive.registerAdapter(ProductModelAdapter());
  // await Hive.openBox<CartItemModel>('cart_box');
  // -----------------------------------------------------------

  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    // Wrap the app in MultiBlocProvider to make AuthBloc available globally
    return MultiBlocProvider(
      providers: [
        // Initialize AuthBloc and trigger the check status event immediately
        BlocProvider<AuthBloc>(
          create: (context) => di.sl<AuthBloc>()..add(AuthCheckStatus()),
        ),
      ],
      child: MaterialApp.router(
        debugShowCheckedModeBanner: false,
        title: 'Shoply',

        // 1. Theme Configuration
        theme: AppTheme.lightTheme,

        // 2. Router Configuration
        routerConfig: AppRouter.router,
      ),
    );
  }
}